const nombre = "Monitor de 20 pulgadas";
const precio = 300;
const disponible = true;

//Un objeto agrupa todo en una sola variable
//En lugar de tener 3 variables podemos crear un objeto que contenga esos 3 valores
//En las variables se utiliza el signo de = y en el objeto se utiliza :

//Object literal
const producto1 = {
    /*Propiedad o llave del objeto*/ /*Valor*/
    nombre: "Monitor de 20 pulgadas",
    precio: 300,
    disponible: true
}

console.table(producto1);

/*
//En lugar de crear diferentes variables, en un objeto puedes agrupar todo en una sola variable
const nombre = "Monitor de 20 pulgadas;
const precio = 300;
const disponible = true;

//Cuando una variable tiene un "=" y llaves "{}" es un objeto

//Object literal

const producto = {
//Izquierda (propiedad o llave del objeto) derecha (valor)
nombre: "Monitor de 40 pulgadas,
precio: 300,
disponible: true
}

console.log(producto);
*/