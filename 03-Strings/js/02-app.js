const producto = 'Monitor 20 pulgadas';
console.log(producto);
//Metodo de los string para saber cuantas letras tiene la palabra
console.log(producto.length);

//Si la palabra tiene la palabra "pulgadas" a partir de que numero
console.log(producto.indexOf('pulgadas'));

//Saber si la palabra existe o no con booleano
console.log(producto.includes('Monitor'));